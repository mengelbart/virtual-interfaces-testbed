{
  "name": "concurrent-dc-test",
  "version": "0.0.1",
  "description": "Automated Tests for concurrently sending data over WebRTC data channels and media streams.",
  "main": "main.js",
  "scripts": {
    "test": "node main.js",
    "jest": "jest --testTimeout 5000 --maxWorkers=1 test/interop/"
  },
  "author": "Mathis Engelbart <mathis.engelbart@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "selenium-webdriver": "^4.21.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
